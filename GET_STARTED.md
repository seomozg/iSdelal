# 🎯 Начните отсюда — Полный путь от разработки до деплоя

Вам нужно продолжить разработку локально, потом выложить на GitHub и деплоить на **test-domain.ru**. Вот пошаговый путь:

---

## 📍 Этап 1: Локальная разработка (СЕЙЧАС)

**Где:** Ваша машина (`c:\Users\HONOR\Desktop\dev\iSdelal`)
**Документация:** [`LOCAL_SETUP.md`](./LOCAL_SETUP.md)

### Быстрый старт:

```powershell
# 1. Убедитесь что Docker Desktop запущен

# 2. Стартуйте все сервисы
cd c:\Users\HONOR\Desktop\dev\iSdelal
docker compose up --build

# 3. В другом терминале смотрите логи
docker compose logs -f backend

# 4. Тестируйте API
curl http://localhost:8081/api/health

# 5. Разработка!
# Изменяйте файлы в backend/app/
# uvicorn автоматически перезагружает (спасибо docker-compose.override.yml)

# 6. Запустите тесты
docker compose exec backend pytest tests/test_api.py -v
```

**✅ Результат:** Локальное приложение работает на `http://localhost:8081`

---

## 📍 Этап 2: GitHub (когда готово к публикации)

**Документация:** [`GITHUB_SETUP.md`](./GITHUB_SETUP.md)

### Шаги:

```powershell
# 1. Инициализируйте Git
cd c:\Users\HONOR\Desktop\dev\iSdelal
git init
git add .
git commit -m "Initial commit: iSdelal RAG kit"

# 2. На GitHub.com создайте репозиторий "iSdelal"
# Settings → mirror the commands GitHub показывает:
git remote add origin https://github.com/YOURUSERNAME/iSdelal.git
git branch -M main
git push -u origin main

# 3. На GitHub добавьте Secret: OPENAI_API_KEY
# Settings → Secrets and variables → Actions → New secret
# Name: OPENAI_API_KEY
# Value: sk-... (ваш ключ)

# 4. Проверьте что GitHub Actions тесты прошли
# Откройте: https://github.com/YOURUSERNAME/iSdelal/actions
```

**✅ Результат:** Код на GitHub, тесты проходят в Actions

---

## 🔄 Если у вас был код на test-domain.ru

**Документация:** [`MIGRATING.md`](./MIGRATING.md)

Если приложение раньше работало на тестовом домене, используйте MIGRATING.md чтобы:
1. Сохранить и восстановить Qdrant коллекции
2. Мигрировать код на локальную машину
3. Настроить GitHub для синхронизации
4. Продолжить разработку локально с теми же данными

---

**Документация:** [`DEPLOY.md`](./DEPLOY.md)

### Первый раз (подготовка сервера):

```bash
# На вашем сервере (SSH):
ssh user@test-domain.ru

# Клонируйте репо
cd /opt
git clone https://github.com/YOURUSERNAME/iSdelal.git
cd iSdelal

# Создайте .env с правильными ключами
cd backend
cp .env.example .env
nano .env
# Установите:
# OPENAI_API_KEY=sk-...
# API_KEY=your-strong-secret-key

cd ..

# Стартуйте приложение
docker compose up --build -d

# Проверьте
curl http://localhost:8081/api/health
```

### Регулярные обновления (после push в main):

```bash
# На сервере:
cd /opt/iSdelal
git pull origin main
docker compose up --build -d
```

**✅ Результат:** Приложение работает на `http://test-domain.ru/api/`

---

## 🔄 Workflow разработки → GitHub → test-domain.ru

```
┌─────────────────────┐
│ Локальная машина    │
│ Разработка & тесты  │
│ git add/commit      │
└──────────┬──────────┘
           │
           │ git push origin main
           │
           ▼
┌─────────────────────┐
│ GitHub              │
│ - Tests (Actions)   │
│ - Code review (PR)  │
│ - Merge to main     │
└──────────┬──────────┘
           │
           │ git pull origin main
           │
           ▼
┌─────────────────────────────────────┐
│ test-domain.ru сервер              │
│ docker compose up --build -d        │
│ API live на test-domain.ru/api/     │
└─────────────────────────────────────┘
```

---

## 📚 Документация по темам

### Разработка
- [`LOCAL_SETUP.md`](./LOCAL_SETUP.md) — подробная инструкция локальной разработки
- [`QUICK_COMMANDS.md`](./QUICK_COMMANDS.md) — шпаргалка с часто используемыми командами

### Миграция со старого сервера
- [`MIGRATING.md`](./MIGRATING.md) — как перенести данные и код с test-domain.ru на локальную машину

### Git & GitHub
- [`GITHUB_SETUP.md`](./GITHUB_SETUP.md) — как создать репо, добавить secrets, запустить Actions

### Деплой
- [`DEPLOY.md`](./DEPLOY.md) — полная инструкция развертывания на test-domain.ru с HTTPS

### Конфигурация
- [`backend/.env.example`](./backend/.env.example) — все переменные окружения
- [`docker-compose.yml`](./docker-compose.yml) — prod конфиг
- [`docker-compose.override.yml`](./docker-compose.override.yml) — dev конфиг (горячая перезагрузка)

### Тестирование
- [`backend/tests/test_api.py`](./backend/tests/test_api.py) — 11 тестов для API
- [`.github/workflows/tests.yml`](./.github/workflows/tests.yml) — GitHub Actions конфиг

---

## ⚡ Самые важные команды

### Локально
```powershell
# Стартовать разработку
docker compose up --build

# Смотреть логи в реальном времени
docker compose logs -f backend

# Запустить тесты
docker compose exec backend pytest tests/test_api.py -v

# Остановить все
docker compose down
```

### GitHub
```powershell
# Создать новую ветку
git checkout -b feature/my-feature

# Коммитить
git add .
git commit -m "Add: feature description"

# Пушить
git push origin feature/my-feature
# Затем создайте PR на GitHub
```

### На сервере
```bash
# Обновить код
cd /opt/iSdelal
git pull origin main

# Пересобрать и запустить
docker compose up --build -d

# Проверить статус
curl https://test-domain.ru/api/health
```

---

## 🔐 Важно про секреты

### ❌ НИКОГДА не коммитьте:
- `backend/.env` (содержит `OPENAI_API_KEY`)
- API ключи в коде
- Пароли

### ✅ Вместо этого:
- Скопируйте `backend/.env.example` в `backend/.env` **локально**
- Заполните переменные в `backend/.env`
- `backend/.env` уже в `.gitignore` (не будет закоммичена!)
- На GitHub используйте **Secrets**: `Settings → Secrets and variables → Actions`
- На сервере создайте `backend/.env` вручную или через Docker secrets

---

## 🆘 Если что-то не работает

| Проблема | Что делать |
|----------|-----------|
| Docker контейнеры не запускаются | `docker compose logs backend` |
| API не отвечает | `curl http://localhost:8081/api/health` |
| Тесты падают в GitHub Actions | Убедитесь что `OPENAI_API_KEY` secret добавлен |
| Горячая перезагрузка не работает | `docker compose restart backend` |
| Не знаете нужную команду | Смотрите [`QUICK_COMMANDS.md`](./QUICK_COMMANDS.md) |

---

## 📋 Чеклист подготовки

### Локально:
- [ ] Docker Desktop установлен и запущен
- [ ] `docker compose up --build` работает без ошибок
- [ ] `curl http://localhost:8081/api/health` возвращает `{"status":"ok"}`
- [ ] Тесты проходят: `docker compose exec backend pytest tests/ -v`

### GitHub:
- [ ] Git репозиторий инициализирован локально
- [ ] Репозиторий создан на GitHub
- [ ] `git push origin main` работает
- [ ] `OPENAI_API_KEY` добавлен в GitHub Secrets
- [ ] GitHub Actions тесты проходят

### test-domain.ru:
- [ ] SSH доступ работает
- [ ] Docker & Docker Compose установлены на сервере
- [ ] Репозиторий клонирован в `/opt/iSdelal`
- [ ] `backend/.env` создан и заполнен
- [ ] `docker compose up --build -d` работает
- [ ] `curl http://localhost:8081/api/health` отвечает

---

## 🚀 Следующие шаги

### Сейчас:
1. Откройте [`LOCAL_SETUP.md`](./LOCAL_SETUP.md)
2. Следуйте инструкциям
3. Запустите `docker compose up --build`

### Когда готово:
1. Откройте [`GITHUB_SETUP.md`](./GITHUB_SETUP.md)
2. Создайте GitHub репозиторий
3. Пушьте изменения

### Для деплоя:
1. Откройте [`DEPLOY.md`](./DEPLOY.md)
2. Следуйте инструкциям по настройке сервера
3. Обновления: `git pull + docker compose up -d`

---

**Вы готовы!** 💪 Начните с [`LOCAL_SETUP.md`](./LOCAL_SETUP.md) →
